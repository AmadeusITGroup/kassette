/**
 * A wrapper around Node.js request objects, known as `IncomingMessage`,
 * for which some specific properties are set only in this case of being a
 * request (and vice versa).
 */

export * from './impl';
export * from './model';
